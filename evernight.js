if (Meteor.isClient) {

}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}

//Accounts.createUser({username:'averrin',email:'averrin@gmail.com',password:'owertryn8',profile:{name:'Averrin'}})

